<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Telemedicine"
        tools:ignore="HardcodedDebugMode"
        tools:targetApi="31">
        <activity
            android:name=".AIDoctorMatchingActivity"
            android:exported="false" />
        <activity
            android:name=".CommunityDiagnosisActivity"
            android:exported="false" />
        <activity
            android:name=".SmartMedicineActivity"
            android:exported="false" />
        <activity
            android:name=".DoctorChatActivity"
            android:exported="false" />
        <activity
            android:name=".ChatActivity"
            android:exported="false" />
        <activity
            android:name=".introactivity"
            android:exported="false" /> <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplasScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Profile Selection Screen -->
        <activity
            android:name=".profileactivity"
            android:exported="false" /> <!-- Login Screen -->
        <activity
            android:name=".loginpage"
            android:exported="false" /> <!-- Signup Screen -->
        <activity
            android:name=".signupactivity"
            android:exported="false" /> <!-- Main Activity (After Login) -->
        <activity android:name=".NearbyMedicalShopsActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>